
## 11:56 News-Publisher PM 執行

- news_publisher.py 19 執行完成
- Telegram ✅ 發送成功
- Notion ❌ 寫入失敗
  - 錯誤：`rich_text[0].text.content.length should be less than or equal to 2000`
  - 位置：` workflows/news_publisher.py` line 127（update 現有頁面時）
  - morning run 成功的同一行程式碼，afternoon run 失敗（同一個 2026-04-23 page）
  - 原因待查：可能是 Notion 欄位設定被改、或 API 行為異常
- 結論：Kai 已收到 Telegram，Notion 待手動補救或等下次成功時更新


## 07:53 News-Publisher-AM cron 執行（cron:3945921b）

- **背景**：同時排程的新聞流（見下方時序）
- **結果**：❌ 失敗 — `找不到 processed 檔案`
  - `processed-20260423-07.jsonl` 尚未生成（News-Aggregator-AM 仍在執行）
  - News-Aggregator-AM 在 07:53 UTC 仍在執行（2020568ms 逾時，批次 28/38 卡住）
  - 原因是 LLM 處理太慢（112則 × 3則/批次 × 240s/批次 超過 900s 硬上限）

**時序重建（2026-04-24）**：
- 07:40 UTC (15:40) — News-Aggregator-AM 啟動
- 07:52 UTC (15:52) — News-Fetcher-AM 完成（230筆 raw 已寫入）
- 07:53:42 UTC (15:53:42) — **News-Publisher-AM cron 觸發**（落後schedule 8分42秒）
- 07:53:40 UTC (15:53:40) — **News-Aggregator-AM 逾時終止**（未產出 processed）
- → Publisher 讀取 processed-20260423-07.jsonl → 失敗

**根本原因**：
1. OpenClaw cron `wakeMode: now` 的 job 在 isolated session 執行
2. 兩個 jobs（Aggregator-AM + Publisher-AM）同時排程只差 2 秒，但 Aggregator 需要 33+ 分鐘
3. Publisher 搶在 Aggregator 完成前啟動 → 讀不到檔案
4. 實際上根本沒有 PM 的 processed file 存在

**待修復**：
- `news_aggregator.py` 的 BATCH_SIZE / MAX_LLM_ITEMS 需要調參，否則每早都會逾時
- 或在 `news_publisher.py` 加檔案存在檢查 + retry 邏輯
- 或 Publisher cron 改到 08:00 以後，確保 Aggregator 有足夠時間完成


## 待修復：News-Aggregator-AM LLM 逾時問題（B6 處理）

**觀察**：
- 執行時間：04/22=31.5min、04/24=33.7min（擊中900s cron 硬上限）
- 執行時間有上升趨勢（指紋庫累積，越來越慢）
- 04/24 的 run summary 自述「逾時」但 cron state 仍顯示 ok（可能是 session timeout 被體制忽略）

**建議修復方案**：
1. `BATCH_SIZE`: 3 → 2（減少每次 token 用量）
2. `MAX_LLM_ITEMS`: 120 → 80（減少總批次）
3. 或在 `news_publisher.py` 加 `wait_for_processed()` retry 機制（最多等 30 min）

**cron 排程衝突問題**：
- News-Aggregator-AM 和 News-Publisher-AM 排程只差 2 秒
- Aggregator 需要 15~34 min 才能完成
- Publisher 在 Aggregator 完成前就啟動 → 讀不到檔案
- 建議：Publisher 改排 08:00 或加檔案存在等待邏輯
