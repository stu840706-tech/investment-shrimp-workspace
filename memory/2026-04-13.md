# 2026-04-13 日誌

## 重大进展

### 新闻系统 Phase 3 - LLM 指纹项目（进行中）

**目标：** 用真正的 MiniMax LLM 做新闻指纹 + 交叉验证

**发现的问题：**
1. MiniMax API 401 问题 → 发现 agent session 里的 token 可用
2. 直接 Python HTTP 呼叫 MiniMax → 回應 empty（server-side 限速？）
3. `max_tokens=2000` 不够 → 改为 `4096` 后 LLM 回應正常

**当前状态：**
- `workflows/news_llm.py` 已写完（14KB）
- `call_minimax()` 函数已测试可用（独立 script 测试成功）
- 但从 `news_llm.py` 内部呼叫同样的 API 回應仍是 empty（原因不明）
- 怀疑：API server-side 偶发性限速 或 请求频率问题

**后续方案：**
- 加 retry 机制（3次，间隔2秒）处理偶发失败
- 保持 `max_tokens=4096`
- 测试 OK 后更新 cron

## 其他完成项目

### 24个月营收资料库（后台执行中）
- PID 51978 后台运行，~110min 预估完成
- 写入了 `state/revenue_history.json`（828KB，1930支股票）

### News Fetcher/Aggregator/Sender
- `workflows/news_fetcher.py` ✅ 已测试（642去重，609过滤后）
- `workflows/news_aggregator.py` ✅ 已测试（规则分类正常）
- `workflows/news_sender.py` ✅ 已测试（Telegram发送成功）

### Cron 排程
```
23 07 * * 1-5  cd /home/ubuntu/.openclaw/workspace && python3 workflows/news_fetcher.py >> logs/news_fetcher.log 2>&1
23 19 * * 1-5  cd /home/ubuntu/.openclaw/workspace && python3 workflows/news_fetcher.py >> logs/news_fetcher.log 2>&1
0 12 * * 1-5   cd /home/ubuntu/.openclaw/workspace && python3 workflows/daily-scan.py >> logs/daily-scan.log 2>&1
```

## 进行中的工作

### LLM Fingerprint（待修复）
- 问题：MiniMax API 直接呼叫回應 empty
- 解决方向：加 retry 电# MiniMax API 直接呼叫回應 empty
- 解决方向：加 retry 电路 + 确认 token 有效性
- 文件：`workflows/news_llm.py`

### 券商报告任务（Task 4）- 暂停，等待 Kai 提供报告内容

## 技术债

- `is_dup()` 函数 aggressive，导致所有指纹被判定为 duplicate → 已修复逻辑
- `max_tokens=2000` 太低 → 改为 `4096`
- 需要为 `news_llm.py` 添加完整的重试机制

## 關鍵決定

1. **Kai 選擇了「繼續做真正的 LLM 指紋」**（選擇 2）- 新闻系统不用纯规则版本上线，继续修 LLM 电路
2. **Subagent spawn 已修復** - 手动编辑 `paired.json` 添加 scopes 后正常工作
3. **MiniMax API key 找到了** - 但直接 HTTP 呼叫有问题，可能需要通过 OpenClaw Agent session

## 待处理

1. 修复 `news_llm.py` 的 MiniMax API 呼叫问题（加 retry）
2. 24个月营收后台进程（PID 51978）完成后验证
3. 确认 Telegram 收新闻（Kai 还没确认是否收到 Phase 3 的报告）
4. 研究 Tpex 上櫃历史营收的资料来源（目前只有1个月数据）